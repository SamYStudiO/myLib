These Easing files are under Adobe license from "fl.transitions.easing" and "fl.motion.easing" packages.
They have been added to myLib to avoid adding all "fl" package to IDE class path when only these files are necessary,
so all transitions management can be achieved with only one framework.